<%= form_for(@invitation) do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% @invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :invited_user_id %><br>
    <%= f.number_field :invited_user_id %>
  </div>
  <div class="field">
    <%= f.label :inviting_user_id %><br>
    <%= f.number_field :inviting_user_id %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.number_field :room_id %>
  </div>
  <div class="field">
    <%= f.label :responded %><br>
    <%= f.check_box :responded %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
